[tool.poetry]
name = "aia-mcp"
version = "1.9.3"
description = "Servidor MCP para el agente aia (amanda-IA)"
authors = ["ranmadxs <ranmadxs@gmail.com>"]
readme = "README.md"
packages = [
    {include = "temperatura"},
    {include = "wahapedia"},
    {include = "monitor"},
    {include = "mcp_cli"},
    {include = "airbnb"},
    {include = "charts"},
    {include = "mcp_email"},
    {include = "mcp_banco_bci"},
    {include = "mangadex"},
    {include = "swagger"},
]

[tool.poetry.scripts]
aia-mcp = "mcp_cli.cli:main"
mcp-swagger = "swagger.server:run_server"

[tool.poetry.dependencies]
python = "^3.11"
mcp = {version = ">=1.0", extras = ["cli"]}
httpx = ">=0.27"
beautifulsoup4 = ">=4.12"
paho-mqtt = ">=2.0"
python-dotenv = "^1.0"
pymongo = ">=4.0"
matplotlib = ">=3.7"
fastapi = ">=0.115"
uvicorn = ">=0.30"
pdfplumber = ">=0.11.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "mcp-swagger-ui (>=0.1.3,<0.2.0)",
    "pytest (>=8.0)",
    "fpdf2 (>=2.8.7,<3.0.0)",
    "pypdf (>=6.14.2,<7.0.0)"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
